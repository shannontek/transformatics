# A new research plan for Navier–Stokes and Transformatics

> **Current priority and source update — 8 September 2026.** The user has shifted the active work to a full, openly attributed Transformatics textbook: mathematical foundations, human–model collaboration history, academic references, and an explanation of the external result. OpenAI has now released a forced Navier–Stokes breakdown paper and formalization. [Pinned source assessment](NSE_OPENAI_RESULT_BRIDGE_2026_09_08.md). FORCED-D is **IMPORTED** (external authorship; native Lean target build passed; independent Comparator not run), while unforced ROOT and E-prime remain **OPEN**. This project did not complete its own singularity construction. Earlier local FORCED-D OPEN labels and private-proof availability statements below are historical. Preserve the research record; do not restart a new research/DNS campaign as part of the textbook task. No public push, visibility change or deployment is recorded by this update.


**Thirteenth execution, 8 September (current):** the
[explicit growing-window theorem](ANALYSIS_NOTES/NSE_UNIFORM_PROFILE_WINDOW_2026_09_08.md)
now covers eta=ell^(1/16), T=t_f+3log(ell)/(16mu), for one fixed
Gevrey-2 profile choice. A quantitatively controlled finite depth
J=ceil(K eta) gives an actual smooth unforced Q_h through T with
approximation gradient error at most h^eta and endpoint added strain
comparable to eta ell². Separate background, complete pressure/mean/profile
and strong-continuation reviews passed, followed by root reads.
The data still vary with h; no next seed or infinite trajectory is supplied.

For arbitrary fixed smooth profiles, the
[diagonal corollary](ANALYSIS_NOTES/NSE_DIAGONAL_WINDOW_2026_09_08.md)
instead gives a sufficiently slow existential eta(h) tending to infinity,
with the same datum at each h and all-power finer-frequency exclusions.
It does not give an arbitrary prescribed growth rate.

The [periodic-cycle speed calculation](ANALYSIS_NOTES/NSE_CYCLE_NEXT_TRANSFER_2026_09_08.md)
proves ||U_mu(tau)||infinity²<=3-3mu tau-(4/5)tau^4 on one uniform
short interval. At a time when its strain energy has increased, maximum
speed has decreased. The textbook now teaches that observable distinction.
These are restricted written analytic results reviewed by AI agents,
not formal PDE certificates or external expert acceptance. ROOT, E-prime
and FORCED-D remain OPEN. No research DNS or public release.

Next: quantify useful nonlinear feedback on the longer same-flow window
and test a genuinely sufficient next-state gain; preserve all pressure,
one-datum and terminal-force obligations. The completed growing window
must not be presented as a cascade or as arbitrary-error polynomial stability.

**External advice, audited on 8 September:** the
[response and two mathematical audits](NSE_EXTERNAL_REVIEW_RESPONSE_2026_09_08.md)
reject the supplied report's relative-stability shortcut, whole-packet norm
ceiling and general seed barrier. The explicit window above instead pays
ordinary energy growth with a quantitatively deeper finite approximation.
This uses a new Gevrey hypothesis and does not validate the report's cap
or its claimed universal obstruction. All global targets remain OPEN.

**Twelfth execution, 8 September (historical):** the signed postfocus source
and its [full-flow band comparison](ANALYSIS_NOTES/NSE_ACTUAL_COARSE_BAND_2026_09_08.md)
passed independent AI-agent reviews and root reads. The same original-data
Q_h generates a measurable intermediate frequency band: endpoint velocity
and symmetric-strain lower scales are h^(7/4+o(1)) and h^(1/2+o(1)), above
its initial band tails. Exact q and linear-Z leakage are separately bounded.
The band is coarser than the supplied receiver. Its generated remainder
still has C1 norm at most C h^(1/96) and a vanishing current-window strain
clock; this supplies no automatic next host or infinite trajectory.

The [opposing/cyclic forced calculations](ANALYSIS_NOTES/NSE_COUPLED_STAGES_REVIEW_2026_09_08.md)
retain the complete nonlinear force and all fixed mixed derivatives.
Instantaneous resonance cancellation leaves a nonzero force time jet;
the supplied cyclic-wave product dissipates and its omitted reverse
couplings are real. These restrictions allow changed interior velocities.

The [actual periodic cycle](ANALYSIS_NOTES/NSE_PERIODIC_CYCLE_TRANSFER_2026_09_08.md)
now evolves those nonlinear interactions without forcing. Its full pressure
jets and uniform local remainder prove finite total enstrophy gain for
sufficiently small nu q/A, while kinetic energy decreases. This extends
the existing activation prototype, whose sine datum is a common translation
of the old cosine datum. No new architecture, large velocity gain or shape
return is asserted. The activation lesson teaches the pressure cancellation
and the finite-time gain inequality.

Next: useful later nonlinear feedback and an actual compatible next state,
with pressure, viscosity, localization and one-datum/terminal-force budgets.
Do not repeat the completed postfocus sign or first activation coefficient.
These are restricted written results, not formal PDE certificates or external
expert acceptance. ROOT, E-prime and FORCED-D remain OPEN. No new research
DNS or public release. The earlier checkpoints below are historical.

**Eleventh execution, 8 September (previous):** three finite results have
passed independent AI-agent reviews and root integration. The
[finite-depth construction](ANALYSIS_NOTES/NSE_FINITE_DEPTH_PROFILE_2026_09_08.md)
improves approximation of the same original-data Q_h. For every fixed
eta>0, beta>3/2 and P>0, its full finer-frequency gradient tail is at most
h^P through t_eta, after finite proof indices are chosen before h tends to
zero. This neither makes the tail zero nor bounds arbitrary cancelling pieces.

The [generated-normal transport](ANALYSIS_NOTES/NSE_NORMAL_SOURCE_TRANSPORT_2026_09_08.md)
now gives an actual nonlinear remainder with normal velocity and strain
lower scales h^(7/4+o(1)) and h^(1/2+o(1)) at fixed small initial rescaled
time. It subtracts exact q-linearized transport and supplies no new donor.
Its coarser output is not excluded by the finer-tail bound. The signed
postfocus adjoint stress integral remains open; more correction depth alone
does not determine its sign or next-stage coupling.

The [three-dimensional forced stage](ANALYSIS_NOTES/NSE_THREED_FORCED_STAGE_2026_09_08.md)
has a complete finite physical force budget. Its
[inherited-host test](ANALYSIS_NOTES/NSE_INHERITED_FORCED_HOST_2026_09_08.md)
preserves real nonnormal gain but prices its nonlinear feedback:
A_new-a<=exp(2Theta_bar)||f(tau)||infinity tau for the specified unchanged
vertical waves and surviving loop, with bounded base clock Theta=lambda tau.
Next: full nonlinear inheritance beyond that one-way interior structure,
with the same complete force through every transition. These are restricted
written results, not formal PDE certificates or external expert acceptance.
ROOT, `(E′)` and FORCED-D OPEN. No new research DNS or public release.

**Previous update, 8 September (tenth execution):** the
[logarithmic-host theorem](NSE_LOGARITHMIC_HOST_2026_09_08.md) now extends
one finite Q_h member, from its unchanged original datum, beyond the receiver
focus. A small fixed eta preserves the nonlinear error margin while rescaled
strain reaches order eta ell². Whole-support pressure/profile correction and
separate strong continuation passed independent AI-agent reviews. The datum
still varies with h; no new independent phase or infinite trajectory follows.
The next calculation is a signed generated or inherited source on this full
time-dependent state, with preparation, damping, support and pressure retained.
The [execution brief](../prompts/nse_next_attempt_2026_09_08.md) states the
specific task and incorporates the third-direction and symmetry audits.
Unforced ROOT, E-prime and smoothly forced FORCED-D remain OPEN. The forced
track must still leave the excluded geometry and meet its complete force budget.

**Previous update, 8 September (ninth execution):** unforced ROOT, `(E′)`
and smoothly forced FORCED-D remain OPEN. The
[later-flow proof](ANALYSIS_NOTES/NSE_LATER_FLOW_ATTEMPT_2026_09_08.md)
extends the same Q_h by c ell^(-27/8) and preserves a transported radius-c k
core with rescaled gradient error O(ell^(-1/4)) for tau<=ell^(-5). The
complete nonlinear source has an upper bound only, and the full-solution
fine-frequency exclusion persists. Next: a signed new receiving contribution
or a substantially longer controlled host; do not redo short persistence.

The [fixed-annulus argument](ANALYSIS_NOTES/NSE_FIXED_ANNULUS_OBSTRUCTION_2026_09_08.md)
excludes a full compact axisymmetric smoothly forced NS breakdown uniformly
away from a fixed axis. A forced construction must escape that geometry.
The [viscous-stage attempt](ANALYSIS_NOTES/NSE_VISCOUS_STAGE_ATTEMPT_2026_09_08.md)
normalizes both scalar channels to the same full diffusion operator, but an
exact model proves that spatial coupling can destroy the return. The
[gain audit](ANALYSIS_NOTES/NSE_FORCE_GAIN_AUDIT_2026_09_08.md) retains
physical velocity and actual damping; arbitrarily high frequency on a
fixed host is not a free improvement. The
[current brief](../prompts/nse_next_attempt_2026_09_08.md) specifies the
remaining geometry, source, velocity-gain and smooth-force obligations.
These results passed separate AI-agent reads at their stated finite or
restricted scope. No complete viscous stage or global solution is in hand.

**Live literature refresh, 8 September:** the [source assessment](NSE_LITERATURE_UPDATE_2026_09_08.md) now covers the alleged internal OpenAI forced-NS proof, its unavailable public argument, Clay's forcing alternatives, the released affine-core and all-order force methods, and other current claims. The [execution brief](../prompts/nse_next_attempt_2026_09_08.md) gives the complete viscous-return and actual unforced source/state calculations to try next. Both global targets remain open.

**Seventh execution, 8 September (prior finite theorem):**
[the original-time nonlinear handover](NSE_ORIGINAL_NONLINEAR_HANDOVER_2026_09_08.md)
places both supplied waves in the initial datum of an actual smooth unforced
NS solution. The finer seed has rescaled initial C1 size O(ell^(-5/8)),
final added strain at least c ell^(11/8), and whole-flight integrated
gradient O(ell^(11/8)). Full fixed-profile outer/inner matching, an actual-q
whole-envelope estimate, complete mean/pressure corrections and a separate
H12 continuation proof give C1 tracking error h^(1/48-o(1)). The source and
assembly passed separate agent reviews; this is a restricted written proof.
The physical viscosity is fixed but the data vary with scale. The finer
wave was already supplied; no new frequency or infinite trajectory is proved.
Direct disjoint/noncancelling preloading of these receivers on a compact
common domain fails C3; sufficient attenuation removes this handover.
Next: a generated or inherited next seed with one smooth datum and a joint
depth/viscosity budget. ROOT and `(E′)` OPEN. No new DNS.

> The following earlier checkpoint is historical; the current update above and next-attempt brief govern.


**Earlier investigation, after the sixth registered result:** the
[next-transfer review index](NSE_NEXT_TRANSFER_REVIEW_2026_09_08.md)
preserves reviewed finite late-data and original-time linearized derivations,
an exact seed-cost reduction, and a conditional whole-flight nonlinear
budget. The cell-scale actual-q localization now has a complete independent
review: it excludes a uniform arbitrary-error bound polynomial in accumulated
strain. These appendices have not changed the registered theorem graph.
The immediate target is weighted matching for the optimized mixed seed on
one fixed actual profile, including its whole-history strain bound, followed
by a uniform depth budget. Exact deformation and the limiting inner connection
are available; the full polarization row powers remain unproved.
The [depth audit](NSE_TRANSFER_DEPTH_BUDGET_2026_09_08.md) excludes one
particular sufficient isotropic certificate, not NS dynamics themselves.
The [fresh literature assessment](NSE_LITERATURE_UPDATE_2026_09_08.md)
distinguishes the forced Euler results from the conditional unforced
Euler candidate. Neither closes the present NS target.

**Sixth execution, 8 September (historical checkpoint):**
[the outgoing-wave theorem](NSE_OUTGOING_WAVE_2026_09_08.md) realizes a
finer packet's gain in the exact viscous linearization around the actual
first-stage NS flow. Its wavelength is h^(3/2), radius h^(5/4), late
interval length ell^(-3/4), and high-pass gain at least c ell^(1/4),
where ell=sqrt(log(1/h)). All-mode error is h^(1/4-o(1)). The proof
includes actual-characteristic phase control, a robust shear transient,
fixed higher derivatives, exact pressure, and real normalization.
The seed is specified at the late time t0. Nonlinear second handover,
a compatible original-time seed and infinite iteration remain OPEN.
Next: a joint seed/transfer/viscosity budget; a family of late seeded
linear estimates is not an infinite trajectory. No new DNS. ROOT OPEN.


**Fifth execution, 8 September:**
[the log-log handover theorem](NSE_LOGLOG_HANDOVER_2026_09_08.md) proves
smooth supplied-seed strain handover for a family of changing initial data
at one fixed physical viscosity. An explicit mean and two harmonic
corrections give residual h^(11/4), integrated strain O(sqrt(log(1/h))),
and a separate H12 continuation bootstrap with C1 error h^(1/24-o(1)).
The initial rescaled perturbation strain tends to zero; its final strain
is at least c sqrt(log(1/h)), at a reference Euler particle location.
This is a restricted written analytic theorem, not a full Lean certificate.
The old absolute high-pass target is still excluded throughout the window.
Next: determine whether the actual outgoing oscillatory wave can amplify
an inherited or generated smaller seed in a controlled second stage.
Its leading gradient is nilpotent despite large symmetric strain, so a
next Floquet amplifier cannot be inferred from the first handover.
Seed supply, an infinite cascade and ROOT remain OPEN. No new DNS.


**Fourth execution, 8 September:**
[the viscous-packet theorem](NSE_VISCOUS_PACKET_2026_09_08.md) proves
finite-time amplification of supplied high-frequency seeds in actual
smooth unforced NS solutions. The initial datum changes with scale.
Its amplification ratio is unbounded, but its absolute high-pass norm
stays below the prescribed activation target throughout the interval;
the perturbation remains small in rescaled C¹. This is not a cascade.
The [wave-handover note](NSE_WAVE_HANDOVER_2026_09_08.md) proves that
single-phase self-cancellation does not yield a generic envelope-rate
error estimate, and that pressure can generate a normal mean component.
Next: a corrected mean/phase packet through an actual strain handover,
with a separate continuation proof. A shorter log-log window is being
examined; it is not supplied by the registered theorems. No new DNS.
ROOT and `(E′)` remain OPEN.


**Prepared:** 8 September 2026 UTC / 7 September Pacific.  
**Standing:** research strategy, not a proof or a new registered theorem. ROOT remains OPEN.  
**Baseline:** `2bad5bd949524e43c4d299e7125d40aad8ec2545`, branch `codex/qgso-prefix-audit-20260904`.  
**Next-model instructions:** [executable first-attempt brief](../prompts/nse_next_attempt_2026_09_08.md).

**Execution update, same day:** the first attempt is now recorded in
[the activation note](NSE_FREQUENCY_ACTIVATION_2026_09_08.md). A classical
all-mode expansion proves a restricted, perturbation-stable local activation
result and bounds distant empty bands over the local interval. The sparse
prototype cannot supply the concentration exponent needed by the shell
mechanism. The next task is the concentrated-packet estimate in that note's
section 6; do not repeat the prototype. ROOT and `(E′)` remain OPEN.

**Second execution update:**
[the concentrated-packet note](NSE_CONCENTRATED_PACKET_2026_09_08.md)
now proves that construction, finite background persistence, and a
short-time activation exclusion with arbitrary high seeds. The
[steady-background note](NSE_STEADY_BACKGROUND_2026_09_08.md) supplies
a logarithmic small-viscosity comparison interval for a fixed compact
Euler profile, together with a pressure-normal carrier obstruction.
The next estimate at that stage was its section 6: returning oblique covectors
on one specified periodic orbit, before any high-seed NS realization.
This supersedes repeating the concentrated packet task. At that stage no unstable carrier was established. The third execution
below supersedes that carrier status; no global theorem is established.

**Third execution, 8 September:**
[the oblique-carrier proof](NSE_GAVRILOV_OBLIQUE_2026_09_08.md) establishes
an expanding inviscid principal-polarization multiplier on selected actual
compact Gavrilov profiles, for every sufficiently large member of the
specified family. The profile threshold is existential. Its exact relative
return permits iteration without a long-period approximation. Original
cone algebra has passed Lean; the analytic geometry and ODE application
remain written proofs. See [formalization and Tao context](NSE_FLUID_LEAN_INTEGRATION_2026_09_08.md).
Next: a localized finite-wavelength comparison to the actual viscous NS
background, with pressure, damping and amplified errors controlled. Fix
one profile before the physical concentration limit. No NS amplification,
infinite cascade, or ROOT closure is established. No new DNS.

## Recommendation

Make the next attempt about **whether an explicitly specified concentrating cascade can be sustained by the full, unforced Navier–Stokes dynamics**. Start with a small number of interacting frequency bands, derive every coupling, and seek an estimate that remains useful when the frequency and number of transfers increase. The decisive object is a controlled trajectory, including the modes generated outside the proposed cascade.

This is my research judgment about the best next use of this repository. It is not a claim that this route is likely to solve the Millennium Problem. Its advantage is that the first mathematical question is concrete, uses structure the existing obstructions omit, and has useful outcomes on either side: a rigorously excluded mechanism or a controlled realization of one step toward a construction.

Keep `(E′)` and O″ as possible endpoints. Do not make another equivalent growth condition the main discovery target. Keep the compact ancient-solution tail problem as the secondary analytic project, with its limited coverage explicit. Do not resume Q-GSO noncollapse, constant sharpening, or a general DNS campaign.

The new outside input is the 2025–26 work on actual NS norm growth and shell-model cascades. It gives a specific comparison problem for the project's Fourier machinery. The new local input is the kinematic-budget obstruction: matching E, Q and D, even using real solenoidal fields, leaves the momentum equation unfulfilled. Together these point toward **quantitative control of omitted dynamics**, rather than another moment identity.

## 1. What the review establishes

The current project has accumulated a substantial collection of useful obstructions. It has not reduced regularity to a demonstrably easier theorem. That is consistent with the latest [route classification](NSE_ROUTE_CLASSIFICATION_2026_09_06.md), although some older entry documents still describe retired routes as active.

This review followed the current route spine, the main retractions, the discipline documents, relevant code, and three previous attempt notes. It did not independently reprove all 356 graph nodes, rerun the static witness search, or reproduce external computer-assisted proofs. Fifty focused repository tests passed; the coverage audit passed with its three existing warnings. Those checks verify the selected pins and tracking, not ROOT.

The external problem remains unsolved on the Clay Mathematics Institute's current [Navier–Stokes page](https://www.claymath.org/millennium/navier-stokes-equation/). Its [official problem statement](https://www.claymath.org/wp-content/uploads/2022/06/navierstokes.pdf) distinguishes whole-space and periodic formulations and regularity and breakdown alternatives. This plan keeps the repository's narrower target: smooth periodic initial data, positive constant viscosity, and no external force. A result in another setting needs an explicit bridge.

### Lessons that should determine the next attempt

| What was tried | What the evidence actually establishes | Consequence for the plan |
|---|---|---|
| Universal static sharp sign | FRAME-HONEST supplies a carefully checked numerical adverse static field. Its continuum certificate is not interval-rigorous. | Stop assuming a favorable sign at every strain maximum. Static fields can test instantaneous claims, not settle an evolution. |
| Scalar energy, growth and standard-criterion ledgers | Geometric and slowly summable ledgers satisfy the selected inputs while evading their proposed closures. The log-corrected example also passes the recorded critical-norm necessary conditions. | A proposed proof must identify an additional NS-specific estimate. These obstructions do not rule out all uses of classical PDE theory. |
| Spatial realization plus E/Q/D balances | A shrinking solenoidal path matches those balances but has nonzero momentum residual; the local energy test excludes that particular path. | Global moments do not certify a PDE trajectory. Another residual detector alone would repeat work already completed. |
| Sharper carrier and dissipation floors | Better constants do not fix the scaling obstruction. | Keep the existing receipts; do not spend another campaign sharpening this chain. |
| Q-GSO/M-GSO price noncollapse | The energy cap bounds the sum of actual completed prices on every trajectory. The unconditional divergent-sum hypothesis is unavailable; its conditional infinite-ladder version restates the desired exclusion. | Retire the route and dependent price/prefix targets. Preserve the identities and measurements. |
| Transfer criterion | The signed criterion is equivalent to continuation on the stated class. | An equivalence can organize a proof, but is not progress without a new independently accessible estimate. |
| Compact ancient limit | Bounded normalized enstrophy supports the repaired compactness argument; a low-amplitude velocity tail remains uncontrolled. | This is a precise partial problem, not coverage of every possible singularity. |
| Spectral monotonicity and a 2D3C example | Prior attempts found adverse short-time signs in a regular symmetry class; the genuinely 3D extension requires an extra enstrophy power. | Check hidden symmetry and retain cumulative dynamics. Large growth in a regular flow is an essential control. |

Primary local references: [cap audit](NSE_QGSO_CAP_VACUITY_2026_09_06.md), [estimate wall](NSE_ESTIMATE_WALL_2026_09_06.md), [kinematic obstruction and local-energy extension](NSE_QGSO_KINEMATIC_BUDGET_OBSTRUCTION_2026_09_04.md), [transfer budget](NSE_QGSO_TRANSFER_BUDGET_2026_09_04.md), [compactness/pressure corrections](NSE_QGSO_PREFIX_AUDIT_2026_09_04.md), and [energy-codimension obstruction](ENERGY_CODIMENSION_LEDGER_2026_07_25.md).

**Logical caution.** Contradicting an a priori upper bound with an independently proved lower bound is a legitimate proof method. The GSO correction does not ban contradiction arguments or show that every possible dynamic lower bound is false. It shows that assuming divergence of the very quantity already capped does not supply an independent mechanism. This plan does not reopen the retired rung-average program.

**Quantifier caution.** Write every proposed constant's dependencies. A finite-horizon bound depending on controlled initial data and viscosity differs from a universal kinematic inequality, a bound depending on an unknown future norm, and a uniform-in-time assertion. Do not inherit claims of equivalence, strict weakness, or greater strength without matching those quantifiers. In particular, the finite-horizon negation of a blow-up necessary condition is not automatically identical to every global version of `(E′)`.

## 2. What the literature changes

The following are primary-source findings, with their scope retained. Dates refer to the paper or version, not search-engine crawl dates. Preprints are identified as such; reading a theorem statement is not independent reproduction of its proof.

| Source and date | Verified scope | Effect on this project |
|---|---|---|
| [Tao, averaged NS blow-up](https://arxiv.org/abs/1402.0290), 2014 preprint / published 2016 | A modified nonlinearity can preserve energy cancellation and still blow up. | A closure relying only on generic energy/harmonic-analysis bounds must identify what distinguishes the actual NS nonlinearity. |
| [Tao, quantitative critical regularity](https://arxiv.org/abs/1908.04958), 2019, revised 2020 | Quantitative higher-norm control assumes a uniform critical spatial L³ bound; its dependence is triple exponential. | Import the concentration/unique-continuation machinery where its hypotheses apply. It does not produce that critical bound from energy. |
| [Barker–Prange, spatial concentration](https://arxiv.org/abs/2003.06717), 2020 / published 2021 | Quantitative concentration under Type I control, and quantitative treatment of bounded L³ sequences near a proposed singular time. | Use exact concentration tests and track their assumptions; do not replace them with a strain-only diagnostic. |
| [Lei–Ren, quantitative partial regularity](https://doi.org/10.1016/j.aim.2024.109654), 2024 | Logarithmic improvement of partial regularity; quantitative regular regions; special axisymmetric applications. | Useful local tools, but a small singular set is not an empty singular set. The repository's O(r) dissipation-cost obstruction still needs a new input. |
| [Miller, strain/vorticity interaction](https://arxiv.org/abs/2407.02691v2), revised 13 April 2026 | An orthogonality identity, a regular model, and criteria for the full equation. | The identity is already imported. The model's global regularity cannot be transferred by dropping the actual redistribution tensor. |
| [Palasek, arbitrary norm growth](https://arxiv.org/html/2509.18595v1), 23 September 2025 preprint | Smooth periodic data bounded in BMO⁻¹, in fact a stronger Besov norm, can yield arbitrarily large globally regular solutions. | Reject bounds depending only on those initial norms in the forms excluded by Theorem 1.1/Corollary 1.5. This does not refute all initial-L³ or H¹ estimates. Include regular amplification controls. |
| [Palasek, elementary shell-model blow-up](https://arxiv.org/html/2605.13827v1), 13 May 2026 preprint | Viscous blow-up for a widely separated Obukhov-type shell model with smooth forcing and rapidly decaying data. Embedding into full NS is unresolved. | Use the model as a concrete stress test. Its dormant high modes are protected by forcing; our unforced PDE must account for their survival or generation internally. |
| [Looi, Princeton seminar abstract](https://www.math.princeton.edu/events/global-regularity-viscous-dyadic-model-navier-stokes-equations-2026-04-02t190000), 2 April 2026 | Announces regularity for a viscous Obukhov model through damping versus activation. The consulted Palasek bibliography lists the work as forthcoming. | A promising comparison, not an imported theorem until the exact manuscript and hypotheses are obtained or the required argument is independently derived. |
| [Barker, quantitative classification](https://arxiv.org/html/2510.20757v3), revised 11 August 2026 | Quantitative singularity constraints for approximately axisymmetric whole-space data; an additional theorem addresses behavior after a putative singular time. | Potentially useful for a specially matched candidate. Theorems 1–2 assume whole-space weighted initial-data bounds and approximate axisymmetry; generic periodic campaign rows do not qualify. |
| [Hou–Wang–Yang, unforced Leray–Hopf nonuniqueness](https://arxiv.org/html/2509.25116v2), September 2025; arXiv v2 March 2026 | The authors give a computer-assisted construction from compactly supported data singular at the origin, in Lᵠ for q<3; solutions are smooth for positive times. | Learn the profile, inverse-operator and tail-error validation method. This is not finite-time blow-up from smooth initial data. Certification code was not run here. |
| [Wang et al., discovery of unstable singularities](https://arxiv.org/abs/2509.14185), 17 September 2025 preprint | Neural-network discovery of unstable profiles for related fluid equations, including Euler with boundary and IPM. High residual accuracy supports subsequent validation. | AI can search structured candidates. Accuracy by itself is not a proof, and these equations/domains are not the project's viscous periodic target. |
| [Albritton–Barker, ancient solutions](https://arxiv.org/html/1811.00502), 2018 preprint / 2019 publication | Theorem 1.2 excludes mild ancient solutions with uniformly bounded spatial L³ along a sequence tending to the remote past. | Provides an exact endpoint for the secondary project; the missing L³ tail must actually be proved. |
| [Tsai, backward self-similarity](https://personal.math.ubc.ca/~ttsai/publications/leray.pdf), 1998 | Excludes backward self-similar NS solutions under the paper's integrability/local-energy hypotheses. | Audit these exclusions before fitting a backward profile. Do not mistake a forward singular-data construction for a permissible backward blow-up profile. |

The bounded search also checked the recent stationary results of [Tan](https://arxiv.org/html/2501.03609v1) and [Cho–Yang](https://arxiv.org/html/2603.23833v1). Their extra low-frequency or annular hypotheses do not follow merely from the compact branch's recorded bounds. No reviewed source supplies the missing general estimate.

**Search limitation:** this is a targeted strategy review through 8 September 2026, not a complete bibliography or a novelty certification. I read the load-bearing theorem statements and selected proofs/mechanisms, with the [Barker–Prange survey](https://arxiv.org/abs/2211.16215) as an orientation source. Search-result claims without a usable primary statement were not promoted. The Hou HTML title-page date differs from its arXiv version metadata; use the pinned version, not that rendered date, for identification.

## 3. Choice of mathematical direction

| Priority | Question | Why it earns the slot | What it would still leave open |
|---|---|---|---|
| **First** | Can a specified cascade's scale activation persist under all unforced NS interactions? | Fresh outside mechanism; matches the Fourier tools; directly addresses the moment-versus-PDE obstruction; has a bounded first calculation. | A result for one packet class does not cover arbitrary singularity geometry. |
| **Second** | Can an NS blow-up limit's inherited dynamics control its low-amplitude L³ tail along backward times? | Already localized to a precise endpoint with a known terminal theorem. | The divergent-normalized-enstrophy branch, and potentially a difficult stationary Liouville subproblem. |
| Endpoint, not default discovery project | Derive `(E′)` or O″ from new spatial dynamics. | A clean route for organizing a successful estimate. | All of the substantive estimate is presently missing. |
| Park | More model fits to rung prices, constant sharpening, pressure-only clocks, static signs, broad DNS. | The current evidence already identifies their limitations. | More examples would not repair the missing mechanism. |

The first choice is deliberately two-sided. A valid negative theorem about a cascade class is useful even if regularity remains open. A controlled positive realization is useful even if it produces only finite amplification. Neither outcome licenses a solution claim. This is more informative than fitting a condition that says solutions cannot blow up if they obey a bound already equivalent to continuation.

## 4. First mathematical work package: activation with all interactions present

### 4.1 Freeze the equation and the question

Work on `(R/2πZ)³` with

\[
\partial_tu-\nu\Delta u+\mathbb P\nabla\cdot(u\otimes u)=0,
\qquad\nabla\cdot u=0,\quad \int u=0,\quad \nu>0.
\]

The projected equation includes pressure through the Leray projector. A physical-space implementation must use the matching Poisson pressure. Use one fixed positive viscosity, not a sequence tending to zero. Use physical integrals consistently with `E=||u||₂²/2` and `Q=||S||₂²`; record any change to Fourier mean normalization.

**First target:** derive a quantitative estimate for generation and amplification of a specified high-frequency packet over a complete activation interval, including the period before visible growth. The estimate must either obstruct the proposed transfer or control its discrepancy from a true NS trajectory. Its constants must expose dependence on frequency separation, amplitude, viscosity, and interaction depth.

Do not posit a uniform bound for arbitrary solutions as the first lemma. Specify a restricted packet class whose hypotheses can be checked and whose limitations can be stated. Define amplitude, packet width, phase, polarization, initial tail, and what counts as activation before testing a candidate. A possible event definition is the first crossing of `||P_j u||₂=θ A_j`, with fixed `0<θ<1` and prescribed amplitude scale `A_j`; this is a frequency-packet event, not a revival of Q-rung pricing.

### 4.2 Start from exact identities, then attack the estimate

For fixed, mutually orthogonal Fourier projections, the shell energy identity is

\[
\frac12\frac{d}{dt}\|u_j\|_2^2+\nu\|\nabla u_j\|_2^2
=-\langle u_j,\mathbb P\nabla\cdot(u\otimes u)\rangle,
\quad u_j=P_j u.
\]

All pairs of input frequencies belong in the right side. Neighbor-only truncation is an ansatz to justify, not an identity of NS.

A useful elementary check uses the orthogonal sharp split `u=ℓ+h`, where `ℓ=P_{≤K}u` and `h=P_{>K}u`:

\[
\frac12\frac{d}{dt}\|h\|_2^2+\nu\|\nabla h\|_2^2
=-\langle h,(\ell\cdot\nabla)\ell\rangle
-\int h^T S(\ell)h\,dx.
\tag{A}
\]

The advection and high-frequency self-interaction cancel in this *total high-pass energy* pairing. They do not vanish from the high-mode equation or from every individual shell. Equation (A) distinguishes new high-frequency input from amplification by lower-frequency strain.

To make the first missing estimate explicit, put `y_K=||h||₂`, `a_K=||S(ℓ)||_{L∞,op}`, `f_K=||P_{>K}𝓟[(ℓ·∇)ℓ]||₂`, and let `λ_K` be the smallest Fourier frequency above K. The standard comparison from (A) is

\[
y_K(t)\le G_K(t,a)y_K(a)+\int_a^tG_K(t,s)f_K(s)\,ds,
\qquad
G_K(t,s)=\exp\!\int_s^t(a_K(\tau)-\nu\lambda_K^2)\,d\tau.
\tag{A1}
\]

**The first substantive lemma to attempt is a bound on the right side of (A1), throughout an activation window, for the chosen full-PDE packet class.** For exclusion it must stay below the prescribed activation amplitude, with explicit scale dependence. Both the propagator and the generated input need control derived from the equation. Assuming either is harmless simply relocates the difficulty. Failure of this absolute-value upper bound does not establish activation; a positive construction needs signed lower bounds and validation. Equation (A1) itself is only the starting comparison, not progress on ROOT.

For a single Fourier coefficient, the exact starting formula is

\[
\widehat u(k,t)=e^{-\nu|k|^2(t-a)}\widehat u(k,a)
-i\int_a^t e^{-\nu|k|^2(t-s)}P_k
\sum_{p+q=k}(\widehat u(p,s)\cdot q)\widehat u(q,s)\,ds,
\tag{B}
\]

where `P_k=I-k⊗k/|k|²` for `k≠0`. Reality and incompressibility must be enforced on the supplied coefficients before computing a residual.

These are standard identities for the work package to rederive and pin. They are not claimed here as new theorems. They expose the substantive question: does the nonlinear input in (B) replace damped initial high-mode seeds, with the right amplitude and phase, while the entire flow remains inside the proposed configuration class?

A strictly separated support has a simple instantaneous selection rule: two inputs below frequency K cannot directly create output above 2K. **Do not assume that gap stays empty.** The PDE generates intermediate frequencies and later interactions. The desired estimate concerns that evolution.

### 4.3 The proposed attempt, in order

1. **Obtain the comparison mechanism exactly.** Extract the external model's forcing, sign restrictions, frequency separation and activation timing. Identify which are choices of the model rather than consequences of NS. If using Looi, first obtain the actual theorem or independently derive the needed restricted estimate; the seminar abstract is not sufficient.
2. **Choose the smallest nontrivial real periodic packet construction.** Begin with two interacting bands and the modes they generate; add a third when needed to represent transfer and backreaction. Use divergence-free vector potentials or explicit Fourier polarizations. Test that the construction has genuinely three-dimensional dependence. Do not assume a small finite mode set is invariant.
3. **Compute the full interaction and pressure exactly.** Separate the desired amplification, its energy-conserving feedback, intermediate-frequency production, and all other terms. Use signed contractions before taking absolute values. Present the first inequality that loses the required power or phase; do not hide it in an unspecified remainder.
4. **Seek an interval estimate.** Use the heat factors in (B), localized transport/commutator estimates, and the actual backreaction. Compare activation time with damping time. Account for the history before activation and between transfers. The target is a gain or an obstruction stable under increasing frequency, not a favorable instantaneous sign.
5. **Evaluate whether the estimate can iterate.** Track its constants as the frequency separation and the number of transfers increase. A loss at each transition may destroy a seemingly small one-step error. The general regularity or blow-up step remains open until this is resolved.

This is genuinely additional work only if step 4 uses momentum dynamics to prove something beyond the existing scalar/spatial budgets. If the output is merely a nonzero residual, an uncontrolled integral, or a rewritten continuation criterion, record the exact failure and change the mathematical approach within this work package.

### 4.4 Required form of a useful result

An **exclusion result** must specify a class of trajectories or approximate configurations and prove that an attempted activation cannot repeat indefinitely in that class. Each hypothesis must either be checked in the construction or proved to persist by NS. If persistence is open, state a conditional class theorem. To infer general regularity, a further theorem must show that every putative singularity enters an excluded class. That coverage theorem is a separate major open problem; it is not supplied by the compact/spread diagnostic.

A **realization result** must control the distance between the approximate construction and an actual solution over the entire interval. For a smooth solenoidal approximation U, use its full projected residual

\[
R_U=\partial_tU-\nu\Delta U+\mathbb P\nabla\cdot(U\otimes U).
\]

Derive an a posteriori stability estimate in a specified strong norm. For example, an Hˢ energy argument with integer `s≥3` can be organized as a scalar comparison of the form

\[
z'\le a_U(t)z+b_s z^2+\|R_U(t)\|_{H^s},\qquad
z(a)\ge\|u(a)-U(a)\|_{H^s},
\tag{C}
\]

with constants and `a_U` justified from the product/commutator estimates on the fixed torus. This is a suggested validation format, not an evaluated bound. More efficient semigroup or rescaled estimates may be necessary. An L²-only error does not protect a strain peak or a high-frequency activation.

For successive validated steps, expose propagation explicitly. If the derived bootstrap gives

\[
e_{j+1}\le L_j e_j+d_j,
\]

then bound

\[
e_J\le e_0\prod_{j<J}L_j+
\sum_{i<J}d_i\prod_{i<j<J}L_j.
\tag{D}
\]

Every term must stay below the chosen tolerance in the norm and rescaling used for that step. Small residuals are insufficient when the propagator amplifies them too much. Nonlinear bootstrap conditions must be verified before using a linear error recursion.

For any future blow-up construction, all finite-depth realizations must lead to **one smooth initial datum, one positive viscosity and one finite terminal time**, with a proved limit and a diverging continuation norm. A sequence of different regular solutions with increasingly large maxima is not enough. An approximate forcing implemented by auxiliary modes must come from the same unforced PDE and obey the full error estimate.

### 4.5 What the first attempt must return

The next model should produce a short mathematical note with:

- A fully specified packet class and one precise proposed estimate.
- An independent derivation of (A)/(B), then the new estimate or its exact failed line.
- A table of scale powers and all constant dependencies.
- The applicable counterexample tests from section 6.
- Either a controlled finite-time result, a rigorous obstruction for the specified class, or a precise unresolved term with evidence that standard bounds do not close it.
- A decision about the next mathematical step based on that result.

A first attempt is not expected to finish the infinite-dimensional theorem. It is expected to reach the first new inequality rather than spend the whole session retelling the program.

## 5. Secondary analytic project: the ancient velocity tail

The repaired compact branch constructs, conditionally on bounded normalized enstrophy along strain-record times, a nontrivial viscosity-one bounded ancient mild solution v on R³. It has uniformly bounded strain enstrophy and an L⁶ representative. See [the repaired argument](NSE_QGSO_PREFIX_AUDIT_2026_09_04.md), sections 4–6.

For a fixed h>0,

\[
\int_{|v|>h}|v|^3\,dx\le h^{-3}\|v\|_6^6.
\]

Thus a concrete sufficient missing result is the existence of `t_k→−∞` with

\[
\sup_k\int_{|v(x,t_k)|\le h}|v(x,t_k)|^3\,dx<\infty.
\tag{T}
\]

Together with the already controlled high-amplitude part, (T) would meet [Albritton–Barker Theorem 1.2](https://arxiv.org/html/1811.00502). That is an imported endpoint, not a new proof here.

The earlier attempt eliminated the remote-past linear heat term but obtained only a logarithmic upper estimate for the nonlinear integral. Interpolating L⁶ with bounded velocity does not fix that estimate. The original periodic L² bound is not uniform after blow-up rescaling. Do not repeat either argument as if it supplied (T).

If this project is activated, first test localized smooth truncations of the cubic velocity functional in the full momentum equation. Retain pressure work, cutoff flux and the terms from the amplitude transition region. Establish estimates uniformly in the spatial cutoff before taking it to infinity. Cubic testing does not enjoy the pressure cancellation of global quadratic energy, and a low-amplitude cutoff need not be convex.

Narrow the first claim to limits actually inherited from the stated periodic blow-up procedure; record any additional inherited property before using it. Do not silently replace that class by all bounded ancient solutions or assume that the inherited property controls the tail. The explicit solenoidal `r^(−3/5)` tail already demonstrates that the available spatial norms alone are insufficient. Even a successful compact-branch theorem leaves the divergent-normalized-enstrophy branch open.

The previous notes were found outside this checkout, in the local 6 September takeover output directory: `navier-stokes-takeover-2026-09-06.md`, `navier-stokes-second-attempt-2026-09-06.md`, and `navier-stokes-cumulative-growth-2026-09-06.md`. They were read in this review; their experiments were not rerun. Their GSO status language predates the cap correction. The mathematical stopping points are summarized here so this plan does not depend on those files being available to a future model.

## 6. Falsification and validation before spending on computation

Apply each counterexample only to the hypotheses it actually satisfies. A scalar ledger cannot refute a theorem that genuinely uses the full momentum equation. An affine flow on R³ can refute a local algebraic step, but is not a periodic finite-energy counterexample.

| Test | What it should detect |
|---|---|
| Geometric blob and slowly summable ledger | A purported closure obtained from the same scalar budget inputs, including a hidden fixed-exponent assumption. |
| ESS-compatible log-corrected scalar ledger | Treating necessary critical-norm blow-up as a sufficient contradiction. |
| Spatial E/Q/D-matched path | A claim that moment matching certifies dynamics. Its known local-energy failure is a control, not a new result. |
| FRAME-HONEST and affine clock jets | Invalid universal static signs; omitted vorticity or pressure terms; confusing local algebra with admissible global solutions. |
| Exact shear/heat and the 2D3C example | A diagnostic that declares growth or transient spectral concentration to be singularity. |
| Genuinely 3D perturbations | Hidden planar/passive-scalar symmetry and unjustified extension of a 2D estimate. |
| Palasek's regular growth construction | An a priori bound using only a norm for which the proposed form is known to fail. Check exact norm and quantifiers, not just the word “critical.” |
| Large frequency separation and growing interaction depth | Constants that cease to be useful, unaccounted intermediate modes, or nonuniform accumulated error. |

Use exact Fourier arithmetic and written estimates first. Existing campaign data may falsify a proposed inequality within their resolution and stored observables; passing finite data does not validate the inequality. Reuse the floor-slack receipt for its recorded quantities. Missing Z/C or clock tensors remain missing.

New numerical trajectories become justified only after a new analytic coupling estimate gives them a question to answer. Before a decisive run, freeze the construction, parameter range, comparison norm, error budget, acceptance rules and failure rules. Use whole-family holdouts for any learned proposal, and include the regular-growth controls. Model residuals and fitting error are separate from discretization and continuum error.

Keep construction N≤64 and the existing verification rules; no N=512 campaign. A widely separated symbolic frequency hierarchy cannot be validated by pretending a small grid resolves all its levels. Prove tail estimates analytically or report the resolution limit. Use interval validation or an equivalent rigorous method for a continuum certificate. PINNs/optimizers may propose fields and amplitudes, but do not certify the inverse operator, the time evolution or the infinite limit.

## 7. A sequence of work blocks with decision gates

These are deliverable boundaries, not predicted completion times or an instruction to start every project at once.

| Block | Work | Exit condition |
|---|---|---|
| **0 — orientation** | Read this plan, the cap correction, route classification and kinematic obstruction; verify the selected pins. | One consistent equation/claim contract. This planning review completed the baseline pass. |
| **1 — first estimate** | Execute section 4 through the full two/three-band interaction and one interval inequality. | A proof, a class-specific obstruction, or the exact uncontrolled interaction. No expensive DNS. |
| **2 — adversarial analysis** | Test scaling, phases, arbitrary generated modes and the relevant counterexamples. Try a different analytical estimate at a documented failure. | A candidate survives with explicit hypotheses, or is retired at its actual scope. |
| **3 — validation, only if earned** | Implement a small targeted experiment and certify a finite-time comparison when feasible. | The actual NS trajectory satisfies the measured claim with a stated error; otherwise no promotion. |
| **4 — iteration** | Prove uniformity across scale and depth, tracking (D) and initial-data compatibility. | A uniform theorem or a precise failed parameter inequality. Finite success alone cannot pass. |
| **5 — terminal theorem** | For regularity, establish coverage of all putative singularities; for blow-up, build the single admissible datum and prove continuation failure. | A complete written argument meeting the original problem statement, followed by independent specialist scrutiny. |

If the primary mechanism fails in a way that teaches nothing further about arbitrary NS flows, take up section 5. If it yields a new class theorem, finish that theorem and its proof before broadening the class. Avoid splitting attention among a dozen renamed closures. Do not interpret an honest failed first estimate as evidence that no proof is possible.

## 8. What Transformatics should become through this work

The repository contains two distinguishable projects: a transformation/observable calculus and a methodology for adversarial PDE research. Its [current discipline document](TRANSFORMATICS_DISCIPLINE.md) is stronger than the older claim that the framework replaces calculus. The code's shift `f↦f∘T`, finite difference `f∘T−f`, and telescoping accumulation should be positioned in their established operator/dynamical-systems context. Telescoping is not a new a priori NS estimate.

The valuable research discipline is the systematic conversion of failed ideas into constraints on the next precise theorem. Develop that as a teachable, measurable method:

1. **Require both truth and information gain.** A correct identity, a useful equivalence, a restricted theorem and a solution are different outputs. A new name or another equivalent criterion is not automatically a new advance.
2. **Make hypotheses executable where possible.** Every claim card should state the equation/domain, solution class, interval, scale normalization, constants and dependencies, known countermodels, genuinely new input, and exact implication to the target.
3. **Track omitted dynamics, not only scalar slack.** A candidate model needs a full residual and a stability estimate; report which component is responsible for failure. The kinematic path is an ideal teaching example.
4. **Review the weakest line independently.** Before polishing an argument, give its load-bearing lemma and assumptions to an independent reader/context tasked with falsifying it. Human PDE review is especially valuable once there is a short precise result. This is a future workflow recommendation, not outreach performed in this review.
5. **Formalize proved mathematics.** Build and inspect the actual theorem target and its axioms. An imported regularity assumption, an out-of-build file, or `True` is not a formal NS proof. Begin with the short algebra/inequalities that the written argument really uses.
6. **Measure the method on held-out cases.** Build a small benchmark from historical false closures, corrected inequalities and positive control proofs. Evaluate whether a model locates the exact error, reproduces the witness, avoids rejecting valid lemmas, and proposes an actually new estimate. Use blinded cases and independent adjudication; do not score the model by agreement with its own labels.

The first discipline deliverable should be one worked case study accompanying the activation estimate: proposal → exact equations → counterexample or proof → validated claim → next obligation. This develops Transformatics while doing mathematics. A large framework rewrite can wait.

There is no supported novelty claim for Fourier analysis, counterexample-guided reasoning, proof graphs, numerical discovery or computer-assisted validation themselves. Any claim that their particular combination improves research should be supported by the benchmark and a reproducible mathematical result.

## 9. Handoff and completion criteria for this planning task

The next capable model should be able to start at section 4 with the [first-attempt brief](../prompts/nse_next_attempt_2026_09_08.md), without reconstructing months of history. It has an equation, a ranked direction, a concrete first calculation, the known objections, relevant external mechanisms, and explicit criteria for promotion or failure.

For a new mathematical result, follow the repository's normal chain: written proof, exact/certified evidence where applicable, focused pin, honest graph entry and claim updates. This plan itself creates no new PROVED node and changes no theorem status.

Verification performed for the review:

```text
50 passed in 19.97s:
  test_nse_qgso_cap_vacuity
  test_nse_route_classification
  test_nse_estimate_wall
  test_nse_qgso_prefix_audit
  test_nse_qgso_transfer_budget
  test_nse_qgso_kinematic_budgets

coverage_audit: OK; 356 nodes
existing warnings: nse-qgso-h2clock, nse-qgso-ksplit,
                   nse-qgso-phoctave-deep (one attack path each)

Equation (A): four exact rational Fourier comparisons passed,
using two three-dimensional solenoidal fields and two spectral cutoffs.
New document local links and math/fence delimiters checked.
```

No new DNS, learned model, external certification run, full-suite repair, publication or remote push was performed. The remaining uncertainty is mathematical: whether the proposed analysis can supply a scale-uniform dynamical estimate. The plan makes that uncertainty testable; it does not hide it behind a claimed reduction.
